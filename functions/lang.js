const chalk = require(`chalk`);
const {
    EmbedBuilder,
    ActionRowBuilder,
    ButtonBuilder,
    ButtonStyle
} = require(`discord.js`);
const error_send = (error, interaction) => {
    
}

module.exports = error_send;